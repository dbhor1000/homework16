public class Bus extends Transport {

    public Bus(String model, String subModel, String color, int year, String country, int maxSpeed) {
        super(model, subModel, color, year, country, maxSpeed);
    }
}
